export interface Property {
    idProperty: Number
    statusProperty: String
    address: String
    zone: String
    spaces: Number
    roomQty: Number
    bathQty: Number
    backyard: Boolean
    grill: Boolean
}
